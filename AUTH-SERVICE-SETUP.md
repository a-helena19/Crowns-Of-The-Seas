# Auth Service Setup

Nach dem ersten Auschecken/Mergen einmalig ausführen:

```bash
docker build -t eateam6-auth-service:latest ./auth-service
```

Danach läuft der Auth-Service automatisch beim Starten des Backends (Play-Button oder `./gradlew bootRun`).
